import { CosechaDirective } from './cosecha.directive';

describe('CosechaDirective', () => {
  it('should create an instance', () => {
    const directive = new CosechaDirective();
    expect(directive).toBeTruthy();
  });
});
